<div style="text-align: center">

     <h1 class="listsh">{{loggedIn.title}} {{loggedIn.username}}'s {{wineType}} Wish List</h1>

     <form action="/wish-list" method="post">
     <select name="wineType" id="sortBy" onchange="this.form.submit()">
     <option value="none" hidden selected>Wine Type</option>
     <option value="Red"> Red Wines</option>
     <option value="White">White Wines</option>
     <option value="Sparkling">Sparkling Wines</option>
     <option value="Rose">Rose Wines</option>
     <option value="Dessert">Dessert Wines</option>
     <option value="Port">Port Wines</option>
     </select>

     </form>

</div>

<div class="listsDivs">

    {{#each userWishList}}

        {{!-- <a href="/wine-details/{{_id}}">
            <img src="{{image}}" alt="{{wine}}">
            </a>
            <h2>{{wine}}</h2>
            <p>{{winery}}</p>
            <p>{{location}}</p>
            <p>{{rating.average}}</p> --}}
        
        {{!-- <a href="/wine-details/{{_id}}"> --}}
            {{!-- <img src="{{image}}" alt="{{wine}}">
            </a>
            <h2>{{wine}}</h2>
            <p>{{winery}}</p>
            <p>{{location}}</p>
            <p>{{rating.average}}</p> --}}
        <div>

            <div id="eachwine">
                    <a href="/wine-details/{{_id}}">
                        <div id="littlewine">
                        <img id="bottle" src="{{image}}" alt="{{wine}}">
                            <h1 style="color: darkred">{{wine}}</h1>
                            <p>From: {{location}}</p>
                            <p>Winery: {{winery}}</p>
                            <p>Rating: {{rating.average}}</p>
                        </div>
                    </a>
                </div>

            <form action="/wine-details/{{_id}}/add-favourite" method="post">
                <button class="listsbuttons" type="submit">Add to Favourites</button>
            </form>
            <form action="/wine-details/{{_id}}/add-tasted-wines" method="post">
                <button class="listsbuttons" type="submit">Add to Tasted</button>
            </form>
            <form action="/wish-list/{{_id}}/delete-wish-list" method="post">
                <button class="listsbuttons" type="submit">Remove from Wish List</button>
            </form>
        
        </div>

    {{/each}}

</div>